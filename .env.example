# --- Kapso (WhatsApp) ---
KAPSO_API_KEY=
KAPSO_PHONE_NUMBER_ID=
KAPSO_WEBHOOK_SECRET=
KAPSO_BASE_URL=https://api.kapso.ai/meta/whatsapp

# --- Fintoc (pagos) ---
FINTOC_SECRET_KEY=
FINTOC_WEBHOOK_SECRET=

# --- General ---
APP_BASE_URL=https://ortodoncia-montemar.vercel.app

# --- Redis (storage de conversaciones y reservas) ---
# Se genera solo al conectar una base de datos "Redis" (Redis Cloud) desde
# Vercel > Storage. Es una connection string estandar (redis://...), no el
# par KV_REST_API_URL/TOKEN del viejo Vercel KV.
REDIS_URL=
